import 'package:flutter/material.dart';

class ResumeBuilderHomePage extends StatelessWidget {
  const ResumeBuilderHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Resume Builder'),
      ),
      body: Column(
        children: [],
      ),
    );
  }
}
